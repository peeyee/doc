1. 表达式 {}
如果表达式{}中只有单一值。则可以去除{}.


2 ;
在单行中书写多行代码时要加上；但是不建议这么做，而是应该分多行书写。因此scala代码统一不加；

3. 函数

（x）=> {do(x)}

参数只有一个的，可以去除左侧参数，变成do(_), _表示任意的参数值。进而可以省略为只保留方法名do


4. 无参方法 do()
调用时，和定义时统一不加（）。



5. 不要重载操作符
因为这很容易造成歧义。禁止使用。



 
