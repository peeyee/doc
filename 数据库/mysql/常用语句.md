* 外键

```sql
select
	constraint_schema,
	table_name,
	column_name,
	constraint_name,
	referenced_table_name,
	referenced_column_name
from
	information_schema.key_column_usage
where
	-- constraint_schema = 'pm_haishu_tradding' and
referenced_table_name is not null;
# 删除外键
alter table `t_qt_bid` drop foreign key `FK_t_qt_project2`;
```

